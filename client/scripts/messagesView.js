var MessagesView = {

  $chats: $('#chats'),

  initialize: function() {
    // use parse.readAll(), GET REQUEST
    // iterate over the data array
    // use renderMessage
  },

  renderMessage: function(Messages) {
    // apped all the messages to $.chat
    // use _.template for security
  },

};